# Jitsi Meet + Docker + Tailscale no Void Linux
## Окончательное, исправленное и правильное руководство (корневой)

В этом руководстве описано **все**, что было сделано, в том числе:
- установка нужных пакетов
- активация сервисов в Void (runit)
- клонировать стек Docker-Jitsi-Meet
- исправление сопоставления портов (127.0.0.1)
- настройка .env
- настройка docker-compose.yml
- проблемы с портом 80 в родном nginx
- Ограничения Tailnet (без Funnel)
- решение через Tailscale Serve (внутреннее)
- окончательный доступ через `https://jitsi.tailf0138e.ts.net`
- нет фиксированного IP, нет открытых портов, нет внешнего DNS

---

## 1. Установите необходимые пакеты.

```bash
xbps-install -Sy docker docker-compose tailscale git
```

Включите сервисы Void:

```bash
ln -s /etc/sv/docker /var/service/
ln -s /etc/sv/tailscaled /var/service/
sv status docker tailscaled
```

---

## 2. Активируйте и аутентифицируйте Tailscale.

```bash
tailscale up
```

Открыть ссылку в браузере, авторизоваться, авторизоваться.

Переименуйте устройство в Tailnet:

```bash
tailscale set --hostname=jitsi
```

Подтвердите DNSName:

```bash
tailscale status --json | grep DNSName
```

Ожидал:

```
"DNSName": "jitsi.tailf0138e.ts.net.",
```

⚠ Важно:
- Если вы НЕ являетесь администратором Tailnet
- Поэтому **Воронка заблокирована**
- Но Internal Serve работает отлично

---

## 3. Загрузите и подготовьте стек Jitsi.

```bash
mkdir -p /opt/jitsi
cd /opt/jitsi
git clone https://github.com/jitsi/docker-jitsi-meet.git
cd docker-jitsi-meet
cp env.example .env
./gen-passwords.sh
```

---

## 4. Настройте `.env` для использования с Tailscale.
(сначала нам нужно подтвердить IP-адрес Tailnet и внутренний DNS)

Прежде чем редактировать `.env`, подтвердите:

**1 — внутренний IP-адрес Tailscale**

```bash
tailscale ip -4
```

Фактический пример:
```
100.75.137.60
```

**2 — Внутреннее DNS-имя машины в Tailnet**

```bash
tailscale status --json | grep DNSName
```

Фактический результат:

```
"DNSName": "jitsi.tailf0138e.ts.net.",
```

Это **настоящий внутренний** домен, созданный Tailscale на основе настроенного имени хоста и идентификатора Tailnet.

Только после этого отредактируйте `.env`:

```bash
nano /opt/jitsi/docker-jitsi-meet/.env
```

Настройте это следующим образом:

```ini
PUBLIC_URL=https://jitsi.tailf0138e.ts.net
ENABLE_LETSENCRYPT=0
DISABLE_HTTPS=1
ENABLE_AUTH=1
ENABLE_GUESTS=1
AUTH_TYPE=internal

XMPP_DOMAIN=meet.jitsi
#XMPP_AUTH_DOMAIN=auth.meet.jitsi
#XMPP_AUTH_DOMAIN_PREFIX=auth
XMPP_MUC_DOMAIN=muc.meet.jitsi
XMPP_INTERNAL_MUC_DOMAIN=internal-muc.meet.jitsi
XMPP_GUEST_DOMAIN=guest.meet.jitsi




```

### Обоснования:
- **PUBLIC_URL указывает на внутреннее имя Tailscale**
поскольку это фактический URL-адрес, используемый для доступа к серверу в Tailnet.

- **HTTPS внутри контейнера отключен, поскольку TLS поступает от Tailscale**
(Tailscale Serve предоставляет встроенный HTTPS, и нам не нужен TLS nginx от Jitsi).

- **Мы не используем Let’s Encrypt, потому что он не является общедоступным или не выпущен воронкой**
и администратор TailNet еще не включил эту функцию, поэтому общедоступного TLS не существует — только внутренний.

---

## 5. Настройте docker-compose.yml.
(очень важно — именно здесь мы исправили самую большую головную боль)

Этот шаг абсолютно ВАЖЕН, потому что именно здесь мы исправили:

- Вместо Джитси появляется Nginx Void
- Конфликт портов 80/8000
- Tailscale Serve жалуется, что «поддерживается только localhost»
- Джитси непреднамеренно обслуживается извне
- бэкэнд не работает на Serve
- необходимость выставлять только локальные
- автоматический запуск контейнера
- подготовка к будущей ВОРОНКЕ, ничего не меняя потом

Веб-служба должна предоставляться ТОЛЬКО на локальном хосте, потому что:

- **Tailscale Serve ТРЕБУЕТ серверную часть на 127.0.0.1**
(текущая версия Serve принимает только localhost, иначе выдает ошибку прокси)
- **Избегает конфликта с nginx Void**, который работает на системном порту 80.
(вот почему там было написано «Добро пожаловать в nginx!»)
- **Гарантирует, что обслуживаются маршруты к Jitsi**, а не к nginx хоста.
- **Предотвращает случайное подключение к Интернету**, поскольку localhost не принимает внешние соединения.
- **Гарантирует будущую совместимость с FUNNEL**, если выйдет TailNet Admin.
- **При использовании `restart: Always` контейнеры запускаются автоматически после перезагрузки**, без дополнительного запуска.

Отредактируйте композицию:

```bash
nano /opt/jitsi/docker-jitsi-meet/docker-compose.yml
```

И оставьте ТОЧНО так:

```yaml
services:

  web:
    image: jitsi/web:unstable
    restart: always
    ports:
      - "127.0.0.1:8000:80"
      - "127.0.0.1:8443:443"

  prosody:
    image: jitsi/prosody:unstable
    restart: always

  jicofo:
    image: jitsi/jicofo:unstable
    restart: always

  jvb:
    image: jitsi/jvb:unstable
    restart: always
```

Краткое объяснение:

- **127.0.0.1:8000 → 80**
→ Порт 80 контейнера существует только внутри, а получатель — 127.0.0.1.
→ Вот почему Tailscale Serve умеет правильно перенаправлять

- **перезапуск: всегда**
→ Если Void перезапускается, Джитси возвращается один
→ Если Docker перезапускается, Jitsi возвращается сама
→ Если происходит отключение электроэнергии, он возвращается один

- **Это на 100% устраняет проблему Void nginx**
- **Это делает Jitsi невидимым в общедоступном Интернете** (что желательно в Tailnet).
- **Это подготавливает все для активации Funnel в будущем с помощью всего лишь одной команды**

Сохраняем и выходим.

---

## 6. Загрузите стек Docker.

```bash
docker-compose up -d
docker-compose ps
```

Убедитесь, что адрес сети **127.0.0.1:8000 → 80**.

Протестируйте интерфейс на сервере:

```bash
curl -I http://127.0.0.1:8000
```

Ожидал:

```
HTTP/1.1 200 OK
Server: nginx
```

⚠ Если вы видели «Добро пожаловать в nginx!», это был nginx от Void.
Этот тест подтвердил правильность бэкэнда Jitsi.

---

## 7. Экспонирование через Tailscale Serve (внутреннее)

Сбросьте все предыдущие правила:

```bash
tailscale serve reset
```

Создайте внутренний прокси:

```bash
tailscale serve --bg http://127.0.0.1:8000
```

Ожидаемый результат:

```
Available within your tailnet:

https://jitsi.tailf0138e.ts.net/
|-- proxy http://127.0.0.1:8000
```

Проверить статус:

```bash
tailscale serve status
```

Tailscale Serve теперь корректно обслуживает Jitsi.

---

## 8. Доступ через Tailnet (работает в ЛЮБОЙ сети)

На вашем ноутбуке, мобильном телефоне, ПК — если вы вошли в Tailscale:

```
https://jitsi.tailf0138e.ts.net/
```

Да:

- HTTPS работает
- Сертификат Tailscale действителен
- Без предупреждения
- Нет пустоты nginx
- Нет: 8000
- Все прямо в красивом домене

⚠ Доступ **только** для участников Tailnet (на данный момент).

---

## 9. Полезные команды

Версии контейнеров:

```bash
docker-compose ps
```

Журналы:

```bash
docker-compose logs -f web
```

Чтобы остановить:

```bash
docker-compose down
```

Статус обслуживания:

```bash
tailscale serve status
```

Сбросить подачу:

```bash
tailscale serve reset
```

---

## 10. Добавление пользователей

```bash
docker compose exec prosody prosodyctl --config /config/prosody.cfg.lua register admin meet.jitsi Jitsi1234
```

Ожидаемый результат:

```
usermanager         info	User account created: admin@meet.jitsi
```

---

## 11. Когда администратор TailNet выпустит FUNNEL (необязательно, публичный доступ)

Если **Администратор TailNet** включит Funnel, вы сможете
Jitsi для всего ИНТЕРНЕТА, с действующим HTTPS, независимо от брандмауэра, модема или фиксированного IP.

Активировав Funnel, вы выполняете:

```bash
tailscale funnel --https=443 http://127.0.0.1:8000
```

И доступ становится:

```
https://jitsi.tailf0138e.ts.net/
```

---

### 🔶 ВАЖНОЕ ПРИМЕЧАНИЕ: КАК ВЫПУСКАТЬ ВОРОНКУ

Только **администратор TailNet** может включить Funnel.

Администратору необходимо сделать:

1. Входить:
https://login.tailscale.com/admin/acls

2. В боковом меню нажмите:
   **Настройки → Воронка**

3. Активируйте опцию:
✔ **Разрешить последовательность действий для этой хвостовой сети**

4. А также активируйте:
✔ выберите устройство **jitsi**
(или имя, которое вы установили с помощью `tailscale set --hostname`)

5. Сохранять.

После этого вы тестируете:

```bash
tailscale funnel status
```

Если он включен, команда перестанет выдавать ошибку, и вы сможете активировать Funnel в обычном режиме.

---

### ✔ Что меняется, когда воронка активна

- Jitsi доступен ПУБЛИЧНО (без TailNet).
- Автоматически действующий HTTPS (с помощью Let's Encrypt от Tailscale)
- URL-адрес остается:
  ```
  https://jitsi.tailf0138e.ts.net/
  ```
- Можно поделиться с ЛЮБЫМ

---

### ✔ Что НЕ МЕНЯЕТСЯ

- Ничто из предыдущего урока не нарушается
- Внутренняя служба продолжает работать
- Докер не нужно модифицировать
- Jitsi не нужно перезапускать

---

## КОНЕЦ
Переделанная конфигурация, чистая, без дыр.
Все в порядке и работает.
